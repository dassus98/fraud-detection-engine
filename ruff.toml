line-length = 100
target-version = "py311"
src = ["src", "tests", "scripts"]
extend-exclude = ["notebooks", "mlruns", "data", "logs"]

[lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "ARG"]
ignore = [
  "E501",  # line length handled by the formatter
]

[lint.per-file-ignores]
# Fixtures as unused args are standard pytest; not a smell.
"tests/**" = ["ARG001", "ARG002"]
# verify_bootstrap uses click.echo for CLI output, which is allowed.
"scripts/**" = []

[lint.isort]
known-first-party = ["fraud_engine"]
combine-as-imports = true

[format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"
